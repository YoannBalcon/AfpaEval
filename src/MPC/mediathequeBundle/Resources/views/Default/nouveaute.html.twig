{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} - Nouveautés {% endblock %}

{% block navbar %}

<div>
    <ul id ="homeNavBar">
        <li><a class="itemNavbar"  href="#">Cadre de vie</a></li>
        <li><a class="itemNavbar"  href="#">Vie municipale</a></li>
        <li><a class="itemNavbar" href="#">Vie locale et sociale</a></li>
        <li><a class="itemNavbar" href="#">Vie associative et culturelle</a></li>
        <li><a class="itemNavbar" href="#">Liens</a></li>
        <li><a class="itemNavbar" href="#">Economie</a></li>
        <li><a class="itemNavbar" id="catalog" href="{{ path ('mp_cmediatheque_homepage') }}">Revenir à l'accueil</a></li>             
    </ul>
</div>
    
    <div class="navBarMedia">
        <div class="navMedia">
            <button class="menuBtn"><i class="fa fa-bars" aria-hidden="true"></i></button>
            <div class="content">
                <a class="itemNavbarMedia" href="#">Cadre de vie</a>
                <a class="itemNavbarMedia" href="#">Vie municipale</a>
                <a class="itemNavbarMedia" href="#">Vie locale et sociale</a>
                <a class="itemNavbarMedia" href="#">Vie associative et culturelle</a>
                <a class="itemNavbarMedia" href="#">Liens</a>
                <a class="itemNavbarMedia" href="#">Economie</a>
            </div>
        </div>
        <a class="itemNavbarMedia" id = "mediaCatalog" href="{{ path ('mp_cmediatheque_homepage') }}">Accueil</a>
    </div>
{% endblock %}

{% block body %}
    <table>
        <thead>
            <tr>
                <th>Aperçu</th>
                <th>Titre</th>
                <th>Auteur</th>
                <th>Ajouté le</th>
            </tr>
        </thead>
        <tbody>
            {% for ouvrage in ouvrages %}
                <tr>
                    <td><img class="photoTitre" src="{{ ouvrage.photo }}"height = "190" width ="150"</td>
                    <td><a href="{{ path('mp_cmediatheque_catalogue', { 'id': ouvrage.id }) }}">{{ ouvrage.titre }} </a></td>
                    <td>{{ouvrage.auteur.prenom}} {{ouvrage.auteur.nom}} </td>  
                    <td>{{ouvrage.date|date("m/d/Y")}}</td>  

                    {% if is_granted("ROLE_USER") and not is_granted("ROLE_ADMIN") %} {% set reserve = false %}
                        {% for dispo in dispos %}
                            {% if dispo.ouvrage == ouvrage %}
                                {% set reserve = true %}
                            {% endif%}
                        {% endfor%}
                        {% if reserve %}
                            <td>Indisponible</td>
                        {% else %}          
                            <td><a href="{{ path('mpc_reservation', {'id': ouvrage.id }) }}">Réserver</a></td>
                        {% endif %}
                    {% endif %}
                    <td>
                        {% if is_granted("ROLE_ADMIN") %}
                            <ul> 
                                <li id="editCatalogue">
                                    <a href="#">éditer l'article</a>
                                </li>                        
                                <li id="editCatalogue">
                                    <a href="#">supprimer l'article</a>
                                </li>                        
                            </ul>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
{% block hours %}
{% endblock %}
