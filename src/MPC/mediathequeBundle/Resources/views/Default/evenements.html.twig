{% for evenement in evenements %}

    <h2>{{evenement.titre}} le {{evenement.date|date("d/m/Y")}}</h2>  
    <p class ="eventPhoto"><img src="{{ evenement.photo }}" width ="420" height="220"></p>
    <p>{{evenement.description}}</p> 
{% endfor %}